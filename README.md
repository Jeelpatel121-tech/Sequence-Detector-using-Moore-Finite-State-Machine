# Sequence-Detector-using-Moore-Finite-State-Machine
This project implements a Moore Finite State Machine (FSM) to detect a specific binary input sequence. In a Moore machine, the output depends only on the present state, not directly on the input.
